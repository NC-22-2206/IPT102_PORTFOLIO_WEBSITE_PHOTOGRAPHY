{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Open Current PHP File on XAMPP",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:8081/IPT102-Portfolio_Website/${relativeFile}",
      "webRoot": "${workspaceFolder}"
    }
  ]
}